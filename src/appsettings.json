{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Connection": {
    "Postgres": "Host=${POSTGRES_HOST};Port=${POSTGRES_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
  },
  "JWT": {
    "SECRET_KEY": "",
    "ACCESS_TOKEN_TTL": 15,
    "REFRESH_TOKEN_TTL": 1440
  },
  "SMTP": {
    "HOST": "",
    "PORT": "",
    "USERNAME": "",
    "PASSWORD": "",
    "FROM_EMAIL": "",
    "EMAIL_SENDER": "",
    "ENABLE_SSL": true
  }
}
